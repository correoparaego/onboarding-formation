# PR1 scaffold only. Authentication reuses Django's built-in User for admins and
# introduces employee token models in Phase 3. The app is registered so the
# project boots and migrations remain consistent across phases.
