"""Shared utilities for the MVP backend.

`common` is a plain Python package on the project path (not a Django app) so it
can be imported by any app's models without being added to INSTALLED_APPS.
"""
